// The equipment designer moved out of Sites into Building Intelligence → Setup.
// This name is kept so a caller that still imports it from here follows the
// move; the one definition is in features/bi/setup/routes.ts.
export { infraDesignerHref } from "@/features/bi/setup/routes";
