# ─────────────────────────────────────────────────────────────────────────────
# DEV override — hot-reload for the Python backend services.
#
# `docker compose` auto-merges this file on top of docker-compose.yml for every
# `up` / `build` run from this directory, so it's active in development WITHOUT
# touching the production-clean base compose.
#
# What it does: bind-mounts each service's source over the image copy and runs
# uvicorn with `--reload`, so editing a .py on the host reloads the worker in
# place — no `docker compose build` needed for backend changes anymore.
#
# The images are editable installs (`uv pip install -e .`), so the mounted source
# IS the imported package. vision also gets the shared `kernel` mounted (it's an
# editable `-e /opt/kernel` install) so kernel edits hot-reload too.
#
# WATCHFILES_FORCE_POLLING: Docker Desktop (macOS) does NOT propagate host inotify
# events across the bind-mount VM boundary, so watchfiles must POLL stat() mtimes
# to see host edits. (Same class of issue as the Traefik file-watch on Docker mac.)
#
# NOTE: the uvicorn command is a SINGLE line on purpose — a YAML folded (`>`) block
# with a more-indented continuation preserves the newline, and in `bash -c` that
# newline terminates the uvicorn command early (dropping the --reload flags).
#
# To run WITHOUT hot-reload (prod-like), use the base file only:
#   docker compose -f docker-compose.yml up -d
#
# It also OWNS every published host port except the gateway's :80. The base file
# publishes the gateway alone, because it is what an appliance install runs: with
# postgres/redis/nats published there, an appliance put its datastores on the
# customer's LAN. Everything a developer reaches directly — psql on :5432, the
# Traefik dashboard, core on :8000 — is restored here.
#
# One of those, `frontend` on :3000, is worth knowing about: it serves the console
# but 404s every /api/v1 call, because the console's API base is same-origin
# relative and only the gateway routes /api. Develop against http://localhost, not
# http://localhost:3000, or logging in fails with "Request failed with status
# code 404" and it looks like a broken backend.
# ─────────────────────────────────────────────────────────────────────────────
services:
  gateway:
    ports: ["80:80", "8090:8080"]              # 8090 = Traefik dashboard

  postgres:
    ports: ["5432:5432"]

  redis:
    ports: ["6379:6379"]

  nats:
    ports: ["4222:4222", "8222:8222"]

  core:
    ports: ["8000:8000"]
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../backend/core/app:/app/app
      # Mount migrations too so newly-added Alembic revisions apply on restart
      # (the start command runs `./migrate.sh` before uvicorn).
      - ../backend/core/migrations:/app/migrations
      # The migrate script is part of the schema bring-up and changes with the
      # migrations, so mount it alongside them rather than baking dev to an old copy.
      - ../backend/core/migrate.sh:/app/migrate.sh
    # `--timeout-graceful-shutdown 5`: THE reason an edit under `backend/core/app`
    # used to hang the container forever. On reload uvicorn asks connections to
    # close and then waits with no timeout at all, so an open SSE relay on
    # `/api/v1/realtime/…` held the worker up and core stopped answering
    # everything — including /auth/login — until somebody restarted it by hand.
    # The relays cooperate now (backend/core/app/core/shutdown.py) so the common
    # case finishes in milliseconds; this bounds the ones that cannot. Shorter
    # than the base compose's 10s because a dev reload is meant to feel instant
    # and there is nothing here worth waiting on.
    command: bash -c "./migrate.sh && exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app --timeout-graceful-shutdown 5"

  vision:
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../backend/vision/app:/app/app
      - ../backend/vision/migrations:/app/migrations
      - ../backend/vision/tests:/app/tests
      - ../backend/kernel:/opt/kernel
    command: bash -c "/usr/local/bin/neubit-migrate 0001_vision_baseline && exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app --reload-dir /opt/kernel/kernel"

  # ── frontends: DEV hot-reload. The base compose builds the PRODUCTION standalone
  # image (node server.js); here we build only the `deps` stage (has node_modules),
  # bind-mount the source over it, and run `npm run dev`. So `docker compose up`
  # (auto-merges this) = dev; `docker compose -f docker-compose.yml up` = production.
  frontend:
    # A DISTINCT TAG, and it matters more than it looks.
    #
    # Without it this `target: deps` build writes to `neubit-v3-frontend:latest` —
    # the very tag the base compose builds the `runner` stage into. Because this
    # override auto-merges on every `up`, running the stack in dev SILENTLY
    # replaced the production image with the deps stage: CMD=[node], no server.js,
    # exits 0 on start, console never comes up. An image payload saved from a
    # developer's machine shipped a console that could not boot, and the base
    # file's "PRODUCTION by default" comment had not been true for months.
    image: neubit-v3-frontend:dev
    ports: ["3000:3000"]                       # serves the UI but 404s /api — see header
    build:
      context: ../frontend
      target: deps
    # NOT a bare `npm run dev`. The entrypoint verifies the container's own
    # node_modules before starting: that it exists, that it was installed from
    # the package-lock.json currently on disk, and that its NATIVE binaries load
    # in this container. It repairs the tree when any of those is false, and it
    # clears a corrupt Turbopack cache if the dev server dies on start.
    #
    # This broke four times in one day and every recovery was manual. The two
    # causes were a volume holding the HOST's macOS binaries in a musl container
    # (a dev server that 500s, or a stylesheet that is 600 bytes of
    # `/* unparsable */` because Tailwind's oxide binary will not load), and a
    # volume seeded once and never refreshed, so a newly added dependency was
    # simply absent no matter how many times the image was rebuilt.
    entrypoint: ["/usr/local/bin/dev-entrypoint.sh"]
    # --webpack, NOT Turbopack, and only here — `npm run dev` stays generic so a
    # native Linux checkout is unaffected.
    #
    # Turbopack's persistent cache does not survive this environment. It failed
    # repeatedly, in three different shapes: a corrupt SST file
    # ("Failed to restore task data"), an endless "Another write batch or
    # compaction is already active", and finally a build that emitted every
    # chunk EXCEPT `chunks/ssr/[turbopack]_runtime.js`, so every route 500'd on
    # `Cannot find module` with a routes-manifest that was never written. That
    # last one reproduced on a brand-new empty volume with 29 GB free, which is
    # what ruled out disk pressure and the macOS bind mount as the cause.
    #
    # webpack dev serves the same app on the first try. The cost is a slower
    # cold start; the benefit is a dev server that comes back up.
    command: ["npm", "run", "dev", "--", "--webpack"]
    environment:
      WATCHPACK_POLLING: "true"
    volumes:
      # NO bind mount of /app, and therefore NO volumes nested inside one.
      #
      # The previous shape was `../frontend:/app` with named volumes at
      # /app/node_modules and /app/.next. Docker Desktop repeatedly re-mounted
      # the host share OVER those nested volumes — at boot, after a Desktop
      # restart, and even mid-run after heavy host-side writes — leaving the
      # container reading the host's darwin node_modules in a musl image. That
      # produced ChunkLoadError reload loops, MODULE_NOT_FOUND 500s, and a
      # container that looked healthy while failing every request. Guards were
      # added to detect it; the honest outcome was a container that refused to
      # start, which is a Bad Gateway for whoever is using the console.
      #
      # So the nesting is gone. node_modules and .next now live in the
      # container's own filesystem — node_modules baked by the `deps` stage
      # from package-lock.json (musl binaries, correct by construction), .next
      # rebuilt per container. Only the things that need hot reload are bound,
      # and none of them is a parent of the other two, so there is nothing left
      # for a re-mount to shadow.
      #
      # Adding a NEW root-level file that Next reads (a new config, say) means
      # adding it here. That is the cost, and it is a smaller one than a dev
      # environment that breaks itself.
      - ../frontend/src:/app/src
      - ../frontend/public:/app/public
      - ../frontend/next.config.js:/app/next.config.js
      - ../frontend/tsconfig.json:/app/tsconfig.json
      - ../frontend/postcss.config.js:/app/postcss.config.js
      - ../frontend/package.json:/app/package.json
      - ../frontend/package-lock.json:/app/package-lock.json

  admin-frontend:
    image: neubit-v3-admin-frontend:dev        # distinct tag — see the frontend note
    build:
      context: ../admin-frontend
      target: deps
    command: npm run dev
    environment:
      WATCHPACK_POLLING: "true"
    volumes:
      # Same shape as the frontend above, for the same reason — read that note.
      # This one was already broken by the shadow ("sh: next: not found", exit
      # 127) because its node_modules had become the host's tree too.
      - ../admin-frontend/src:/app/src
      - ../admin-frontend/public:/app/public
      - ../admin-frontend/next.config.js:/app/next.config.js
      - ../admin-frontend/jsconfig.json:/app/jsconfig.json
      - ../admin-frontend/postcss.config.js:/app/postcss.config.js
      - ../admin-frontend/tailwind.config.js:/app/tailwind.config.js
      - ../admin-frontend/package.json:/app/package.json
      - ../admin-frontend/package-lock.json:/app/package-lock.json

  # ── reading-writer: DEV hot-reload. Same pattern as the other Python services.
  # The pipeline lives in the lifespan, so a reload tears the JetStream consumer
  # down and rebuilds it — which is safe (durable consumer, nothing acked that was
  # not written) but does mean an edit costs a few seconds of consumption. The
  # `reporting` package is mounted too: it owns the schema this service imports.
  reading-writer:
    # /health, /readyz, /metrics, /stats. Development only: the base file is what
    # an appliance runs alone, and an unauthed operational surface does not belong
    # on a customer's LAN.
    ports: ["8020:8000"]
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../backend/reading-writer/app:/app/app
      - ../backend/reporting:/opt/reporting
      - ../backend/kernel:/opt/kernel
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app --reload-dir /opt/reporting/reporting

  # ── reporting-projector: DEV hot-reload. Same pattern as the reading-writer.
  # The consumers live in the lifespan, so a reload tears every durable consumer
  # down and rebuilds it — safe (nothing is acked that was not written) but it
  # costs a few seconds of consumption per edit.
  reporting-projector:
    ports: ["8021:8000"]        # development only - see reading-writer
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../backend/projector/app:/app/app
      - ../backend/reporting:/opt/reporting
      - ../backend/kernel:/opt/kernel
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app --reload-dir /opt/reporting/reporting

volumes:
  # The dev containers' own node_modules and build caches. Named so they can be
  # inspected and dropped by name when something does go wrong — see the
  # frontend service's note.
  # frontend_node_modules / frontend_next / admin_frontend_node_modules /
  # admin_frontend_next were REMOVED 2026-09-02. They were volumes nested
  # inside a bind mount, which Docker Desktop kept shadowing; both frontends
  # now take node_modules and .next from their own image layer instead. Drop
  # the leftovers with:
  #   docker volume rm neubit-v3_frontend_node_modules neubit-v3_frontend_next \
  #     neubit-v3_admin_frontend_node_modules neubit-v3_admin_frontend_next
