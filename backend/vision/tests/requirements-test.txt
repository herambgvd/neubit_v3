# Test-only deps. Kept out of the image — a test runner has no business in an
# artifact that ships to a customer.
#
# asyncio_mode = "auto" (pyproject) runs the `async def test_*` coroutines with no
# per-test marker, which is why nothing here is marked.
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20
httpx>=0.27
pytest-cov>=5.0          # coverage.xml for SonarQube — see run-tests.sh --coverage
