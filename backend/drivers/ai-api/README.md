# driver: ai-api

External AI analytics API integration (third-party cameras). Implements the capability contract, publishes normalized events to NATS.
protocol → api → brand. See ../../../docs/ARCHITECTURE.md section 5.
