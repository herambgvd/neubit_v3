# driver: iot

IoT — Modbus (Go), BACnet (Python sidecar). Implements the capability contract, publishes normalized events to NATS.
protocol → api → brand. See ../../../docs/ARCHITECTURE.md section 5.
