# driver: access

Access controllers — OSDP, SignalR (one brand). Implements the capability contract, publishes normalized events to NATS.
protocol → api → brand. See ../../../docs/ARCHITECTURE.md section 5.
