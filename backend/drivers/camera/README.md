# driver: camera

ONVIF/RTSP nvr-estate:3rd-party NVR (ONVIF-G/Hik/Dahua) ai-api:external AI analytics API access:OSDP/SignalR fire:MQTT/panel iot:Modbus(Go)/BACnet(Python sidecar). Implements the capability contract, publishes normalized events to NATS.
protocol → api → brand. See ../../../docs/ARCHITECTURE.md §5.
