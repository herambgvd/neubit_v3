# Electron pulls its binary from a separate mirror; keep installs reproducible.
save-exact=false
# Uncomment behind a corporate proxy / air-gapped mirror:
# electron_mirror=https://npmmirror.com/mirrors/electron/
